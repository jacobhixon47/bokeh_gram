<% if @images.any? %>
  <% @images.each do |image| %>
    <h1><%= image.caption %></h1>
    <%= link_to image_path(image) do %>
      <%= image_tag image.picture.url(:medium) %>
    <% end %>
  <% end %>
<% end %>
